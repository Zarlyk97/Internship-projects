import 'package:flutter/material.dart';

class AudioPlayerListPage extends StatelessWidget {
  const AudioPlayerListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      appBar: null,
    );
  }
}
